---
title: 卷五写作卡片
status: draft
updated: 2026-04-07
---

# 卷五写作卡片

> 用途：作为新卷五的内部写作卡片。先按“扩展能力总图”搭骨架，再从旧文和源码里抽素材，禁止让旧文反向决定新文章结构。

---

## 卷五当前结构总览

当前确定：

- 前段总论：**3 篇**
- skills：**5 篇**
- MCP：**3 篇**
- **Agent 主轴（含 subagent / fork worker）**：**7 篇**
- hooks：**3 篇**
- plugins：**3 篇**
- 卷尾收束：**1 篇**

**合计：25 篇**

对象主线顺序固定为：

> **skills → MCP → Agent 主轴 → hooks → plugins**

---

## 01｜为什么复杂场景会逼 Claude Code 长出扩展层
- **主问题**：复杂场景为什么会逼出扩展层
- **核心判断句**：复杂场景不是边缘案例，而是 Claude Code 必须适应的常态；扩展层正是在这里变成必要能力。
- **必须完成**：立卷五总问题；区分卷三 / 卷四 / 卷五；让读者先理解扩展层为什么必要
- **不讲什么**：不做对象百科；不提前比较所有扩展机制；不写成宣传文

## 02｜为什么 Claude Code 选择把扩展权交给用户
- **主问题**：为什么不是产品团队不断内建，而是把扩展权交给用户
- **核心判断句**：Claude Code 的强，不在于预装了多少能力，而在于它把扩展权交给了用户。
- **必须完成**：把复杂场景压力推进成平台化选择；为 skills / MCP / agent 主轴 / hooks / plugins 铺前提
- **不讲什么**：不写开放平台宣言；不抢对象总地图篇；不提前深讲命令入口

## 03｜skills / MCP / agents / subagents / hooks / plugins 是怎样接入 Claude Code 的
- **主问题**：这些扩展对象怎样接入 runtime，为什么不能混成一个扩展大桶
- **核心判断句**：这些对象不是并排功能点，而是一组以不同方式接入 runtime 的平台对象。
- **必须完成**：给卷五后半建立统一总地图；以 runtime 接入关系为骨架；防止后文平铺介绍
- **不讲什么**：不把各对象正文讲完；不展开命令层与产品入口；不写术语词典

---

# Skills 组（04-08）

## 04｜为什么 skills 不是“长 prompt”那么简单
- **主问题**：为什么 skill 不是更长一点的 prompt
- **核心判断句**：skills 不是提示词长度问题，而是 Claude Code 把用户能力组织成运行时单元的一种方式。
- **必须完成**：打掉误解；立住 skills 系统地位；给后续 4 篇铺起点
- **不讲什么**：不提前展开运行链；不写成规范文；不吃掉 agent 边界

## 05｜skills 是怎样把用户经验、流程和角色结构接进 Claude Code 的
- **主问题**：用户工作方式怎样通过 skills 进入系统
- **核心判断句**：skills 最重要的价值，不是让 prompt 可复用，而是让用户自己的工作方法可以稳定进入系统。
- **必须完成**：立住能力定制重轴；把“用户工作方式进入系统”讲清楚；为 06 / 07 / 08 铺路
- **不讲什么**：不写成方法论散文；不提前深讲 frontmatter；不抢 agent 主线

## 06｜SkillTool / skills runtime 是怎样接进执行链的
- **主问题**：SkillTool / skills runtime 怎样真正接进执行链
- **核心判断句**：skill 不是外挂文档，而是能够被 runtime 正式识别、加载和推进的执行组织单元。
- **必须完成**：从卷三接执行层关系；解释 SkillTool 的 runtime 位置；让读者看见 skill 如何进入工作主链
- **不讲什么**：不把所有设计原则都塞进来；不转去讲命令入口；不吃掉卷六 interface 问题

## 07｜什么样的 skill 才算好的 runtime skill
- **主问题**：什么样的 skill 才能真正稳定进入 Claude Code 工作流
- **核心判断句**：好的 runtime skill，不只是写得漂亮，而是能稳定承担职责、守住边界，并在工作流里持续发挥作用。
- **必须完成**：把最佳实践升成质量判断；解释什么样的 skill 真正适合 runtime；承接 good-runtime-skill / skillify
- **不讲什么**：不写成 checklist；不把卷六 frontmatter interface 讲成主角；不替 agent 组回答执行者问题

## 08｜skill、tool、agent 三者的边界到底是什么
- **主问题**：skill / tool / agent 各自是什么层级
- **核心判断句**：skill、tool、agent 分别承担能力组织、执行能力和执行者结构，不是三种随便互换的名字。
- **必须完成**：给 skills 组做边界收口；为 MCP / Agent 主轴铺更稳坡度；防止卷五后半对象互相吃掉
- **不讲什么**：不抢 Agent 主轴正文；不重写卷三工具主链；不写成术语手册

---

# MCP 组（09-11）

## 09｜为什么 MCP 不是“多了一批远程工具”
- **主问题**：为什么 MCP 不能只理解成远程工具扩容
- **核心判断句**：MCP 的意义不只是远程工具扩容，而是让 Claude Code 拥有稳定接入系统外能力源的通道。
- **必须完成**：打掉浅理解；把 MCP 从工具目录抬到能力源接入层；为 10 / 11 铺路
- **不讲什么**：不写协议教程；不把所有资源类型提前讲完；不和 plugins 混掉

## 10｜Claude Code 是怎样通过 MCP 接入外部能力源和资源系统的
- **主问题**：Claude Code 怎样通过 MCP 接入外部能力源和资源系统
- **核心判断句**：MCP 真正重要的地方，不在“连上去”，而在它让系统外部能力可以被稳定编入 runtime。
- **必须完成**：讲清接入位置；解释能力源 / 资源系统 / runtime 的关系；让读者看见它为什么属于卷五主线
- **不讲什么**：不细讲所有协议实现；不吃掉 hooks / plugins；不写接入教程

## 11｜MCP 和 skills / hooks / plugins 分别是什么关系
- **主问题**：MCP 和 skills / hooks / plugins 各处于什么层级
- **核心判断句**：MCP 解决的是外部能力源接入，不等于工作方法封装，也不等于运行时接缝或平台扩展封装。
- **必须完成**：给 MCP 组收边界；防止 MCP 吃掉 hooks / plugins；让卷五对象分工更稳
- **不讲什么**：不重复 09 / 10；不抢 plugins 组成熟封装问题；不写成对象字典

---

# Agent 主轴（12-18）

## 12｜为什么 agent 不是“另一个工具”
- **主问题**：为什么 agent 不能被理解成特殊一点的工具
- **核心判断句**：agent 不是另一个工具，而是 Claude Code 开始长出更多执行者时出现的结构性对象。
- **必须完成**：打掉误解；先立执行者层；为 13-18 铺起点
- **不讲什么**：不重讲卷三工具主链；不把 worker / fork 全讲完；不写成 agent 目录导览

## 13｜Claude Code 是怎样长出更多执行者的
- **主问题**：Claude Code 怎样从单执行者走向更多执行者体系
- **核心判断句**：agent 的出现意味着系统开始把执行能力进一步外化成多个可分工的执行者。
- **必须完成**：解释执行者扩展重轴；讲清“更多执行者”如何成立；让读者看见卷五的多代理坡度
- **不讲什么**：不细讲 worker / fork 组织细节；不写案例堆积；不提前吃掉 hooks / plugins

## 14｜runAgent 为什么像一条 agent runtime 装配线
- **主问题**：runAgent 在 agent 主轴里到底扮演什么角色
- **核心判断句**：runAgent 不是轻薄包装层，而是把 agent 定义、上下文、工具池、权限、技能、MCP、hooks 组装成可运行体的装配线。
- **必须完成**：把执行者主轴从概念推进到 runtime 主干；解释 agent 定义如何变成可运行体；为 worker / fork 铺路
- **不讲什么**：不写成单纯函数解说；不提前吃掉 fork worker 正题；不变成 skill 组重写版

## 15｜为什么主 agent 还要派生 subagent
- **主问题**：已经有 agent 了，为什么还需要 subagent / worker
- **核心判断句**：subagent 不是可有可无的附属能力，而是主执行者面对复杂任务时进一步展开协作结构的必要结果。
- **必须完成**：解释 subagent 为什么必须进入 agent 主轴；回答“已有 agent 为什么还不够”；立住 worker 分叉的必要性
- **不讲什么**：不提前把 fork 路径细节讲完；不写成并行执行技巧文；不抢信息回流篇

## 16｜forkSubagent 为什么不是“再开一个 agent”
- **主问题**：forkSubagent 和普通 agent 调用差在哪
- **核心判断句**：forkSubagent 干的不是再选一个 agent 去做事，而是在当前上下文上直接切出一个继承父 prompt / 工具面 / 上下文的 worker 分支。
- **必须完成**：讲清 worker / fork 的特殊性；解释为什么它是 agent 主线内部的特殊形态；把 prompt cache / 继承上下文 / worker 约束这些关键点立住
- **不讲什么**：不写成缓存优化技巧篇；不扩成全部 coordinator 问题；不重写 runAgent 主干

## 17｜主 agent / worker agent 的职责边界与信息回流
- **主问题**：主 agent 与 worker agent 的职责边界是什么，结果怎样回流
- **核心判断句**：worker 之所以是协作结构而不是失控分叉，关键在于职责边界和信息回流被系统收得住。
- **必须完成**：给 worker / fork 后半段收口；解释边界与回流为什么重要；把 subagent 放回可控协作结构里
- **不讲什么**：不写成日志追踪教程；不抢 hooks 组事件接缝问题；不重讲 agents 总体价值

## 18｜agent、skill、tool 之间的边界和协作关系
- **主问题**：agent、skill、tool 各自是什么层级，它们怎样协作
- **核心判断句**：tool 是执行能力，skill 是能力组织，agent 是执行者结构；三者相关，但绝不是同一类对象。
- **必须完成**：给整个 Agent 主轴做边界收口；把 agent 与 skills、tools 切清楚；回扣第 08 篇但站在执行者主轴一侧收束
- **不讲什么**：不重讲 skills 组正文；不写成术语百科；不抢卷六 command / tool / skill / agent 总边界

---

# Hooks 组（19-21）

## 19｜为什么平台层不仅要有能力对象，还要有运行时接缝
- **主问题**：为什么平台层除了能力对象外，还必须有运行时接缝
- **核心判断句**：平台化不只意味着对象变多，也意味着 runtime 必须留出可观察、可注入、可干预的接缝。
- **必须完成**：先立 hooks 存在理由；回答“为什么不是 skills / plugins 就够了”；给 hooks 三篇开稳入口
- **不讲什么**：不提前分类讲完所有 hook；不把 hooks 写成配置项索引；不吃掉 plugins 组

## 20｜Claude Code 的 hooks 在 runtime 里到底扮演什么角色
- **主问题**：hooks 在 Claude Code runtime 里到底扮演什么角色
- **核心判断句**：hooks 不只是额外插入动作，而是平台层留给运行时观察、注入和干预的结构化接缝。
- **必须完成**：讲 hooks 的系统位置；解释它和其它扩展对象的关系；为下一篇分类解释做铺垫
- **不讲什么**：不写成 hook 类型列表；不吃掉 plugins 封装层问题；不转去讲卷六命令入口

## 21｜Claude Code 里的各类 hooks 分别在拦什么、接什么、改什么
- **主问题**：不同 hooks 类型分别在拦什么、接什么、改什么
- **核心判断句**：hooks 真正有价值的地方，不在“有 hook”，而在不同 hook 类型各自卡在不同接缝上、承担不同干预职责。
- **必须完成**：把每类 hooks 都解释一下；让 hooks 组不止停在总论；给卷五对象地图增加足够细度
- **不讲什么**：不写成 API 参考手册；不细讲每个配置字段；不替 plugins 组回答封装问题

---

# Plugins 组（22-24）

## 22｜为什么有了 skills / MCP / hooks 之后，系统还需要 plugins
- **主问题**：既然已有 skills / MCP / hooks，为什么还需要 plugins
- **核心判断句**：plugins 之所以存在，不是重复已有扩展对象，而是把更完整的扩展封装形态引入平台层。
- **必须完成**：立住 plugins 的存在理由；回答“为什么不是已有对象拼起来就够了”；给后两篇铺路
- **不讲什么**：不写成生态宣传文；不把所有对象边界重新讲一遍；不提前把封装 / 分发 / 复用全吃完

## 23｜plugins 和其它扩展对象分别处在什么层级
- **主问题**：plugins 和 skills / hooks / MCP 分别处在什么层级
- **核心判断句**：plugins 不是兜底概念，它所代表的是更完整的扩展封装层级，而不是所有扩展对象的统称。
- **必须完成**：给 plugins 组立清边界；把它和 skills / hooks / MCP 区分开；让这组不止停在“也有 plugin”
- **不讲什么**：不重讲 hooks 接缝问题；不吃掉第 24 篇成熟封装问题；不写成对象字典

## 24｜plugins 为什么代表更完整的封装、分发和复用形态
- **主问题**：为什么说 plugins 代表更完整的封装、分发和复用形态
- **核心判断句**：plugins 的重要性，在于它意味着平台层开始具备更成熟的扩展封装、分发和复用能力。
- **必须完成**：把 plugins 从“另一个对象”提升到“平台成熟度的一层表现”；解释封装 / 分发 / 复用的意义；为卷尾收口
- **不讲什么**：不写成生态路线图；不重复 22 / 23；不提前吃掉卷六产品整合问题

---

## 25｜为什么这些扩展对象最终会收成一层平台能力
- **主问题**：这些分散对象为什么最终会共同构成 Claude Code 的平台层
- **核心判断句**：Claude Code 的强，不只在它会执行，而在它能把能力源、工作方式、执行者和接缝层接成长久可用的平台结构。
- **必须完成**：把卷五全部对象重新压回扩展能力总图；说明它们共同构成的是平台能力；自然导向卷六
- **不讲什么**：不逐个重讲对象正文；不写成用户操作指南；不提前把 runtime 命令入口讲成正题

---

## 当前写作约束

1. **先画扩展能力总图，再写对象正文。**
2. **前 3 篇总论不能过厚，重点是立问题，不是提前吃掉后文。**
3. **skills / Agent 主轴是双核心，不能被写轻。**
4. **Agent 主轴内部必须显式解释：subagent / fork worker 是 agent 主线的后半段，不是另一个平级对象组。**
5. **hooks / plugins 都升到 3 篇，并且 hooks 必须解释各类 hooks。**
6. **卷五重点始终是扩展层如何成立，不是命令入口如何暴露。**
7. **卷五的对象必须放回 runtime 关系里讲，不能平铺成功能清单。**
8. **先按 skeleton-first 写作：先职责、再边界、再骨架、最后回收旧文素材。**
